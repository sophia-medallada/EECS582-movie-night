import React from 'react';
import { Box, Typography } from '@mui/material';

const LoginPage = () => {
  return (
    <Box p={4}>
      <Typography variant="h4" gutterBottom>Login</Typography>
      <Typography>login placeholder</Typography>
    </Box>
  );
};

export default LoginPage;
